# A Monolith Code Sample Project with TypeScript + Jest + Clean Architecture

A project 

## How to run?

Go to the root folder and run:

```bash
npm install
```

Then, run:

```bash
npm run start
```
